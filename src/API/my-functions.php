<?php

/**
 * Placez dans ce fichier les fonctions qui concernent votre application.
 * Vous pouvez utiliser les variables déclarées dans les fichiers core/*.php ici.
 * Par exemple, pour $_dbh :
 *
 *      fonction clean($input) {
 *          global $_dbh;
 *          $output = mysqli_real_escape_string($_dbh, $input);
 *          return $output;
 *      }
 *
 */
